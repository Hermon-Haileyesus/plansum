import { globalStyles } from "@/css/styles";
import { Text, View } from "react-native";

export default function AddPlanScreen() {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.title}>Add Plan</Text>
    </View>
  );
}
